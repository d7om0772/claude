# النشر على السحابة

المشروع يعمل بعملية واحدة: نفس الخادم يقدّم الواجهة ويُنفّذ الرندر، على
منفذ واحد. لا وكيل ولا خادم ثانٍ.

## التشغيل محلياً أولاً (للتأكد قبل النشر)

```bash
npm install
npm run build      # يبني الواجهة الساكنة
npm start          # الخادم على المنفذ 5174 (أو PORT)
```

افتح `http://localhost:5174`. للتطوير مع إعادة التحميل الحيّ استعمل
`npm run dev` بدلاً منه (واجهة على 5173 وخادم على 5174).

## Docker

```bash
docker build -t montage-templates .
docker run -p 8080:8080 montage-templates
```

الصورة تثبّت اعتماديات Chrome، وتنزّل متصفح الرندر **وقت البناء** لا وقت
أول طلب — فأول رندر سريع ولا يحتاج شبكة أثناء التشغيل.

## على مضيف سحابي

أي مضيف يشغّل حاويات: Fly.io، Railway، Render، Google Cloud Run، أو خادم
افتراضي عادي. يكفي أن يقرأ `Dockerfile`.

```bash
# مثال Fly.io
fly launch --dockerfile Dockerfile
fly deploy
```

المضيف يحقن متغيّر `PORT` والخادم يقرأه (الافتراضي 5174، وفي الصورة 8080).

### الحد الأدنى للموارد

الرندر يشبع المعالج ويستهلك ذاكرة. **لا تنشر على أصغر باقة.** ابدأ من:

| المورد | الحد الأدنى | مريح |
|---|---|---|
| المعالج | نواتان | ٤ أنوية |
| الذاكرة | ٢ غيغا | ٤ غيغا |
| القرص | ٥ غيغا | ١٠ غيغا |

المهام تُنفَّذ واحدة تلو الأخرى عمداً: الرندر يشبع المعالج، وتشغيل مهمتين
معاً يبطئ الاثنتين ولا يقصّر الزمن الكلي. المستخدم يبقى حرّاً يشتغل على
قالب ثانٍ لأن الطابور في الخادم لا في الواجهة.

## نقاط تحتاج قراراً قبل نشر عام

**التخزين مؤقت.** النواتج في `.render/out` والمرفوعات في `public/uploads`،
وكلاهما داخل الحاوية. أي إعادة نشر أو إعادة تشغيل تمسحهما. للاستعمال
الجادّ اربطهما بقرص دائم (`fly volumes`) أو بتخزين كائنات مثل S3.

**تاريخ المهام في الذاكرة.** إعادة تشغيل الخادم تنسى قائمة المهام حتى لو
بقيت ملفاتها. ملف JSON صغير يكفي لو أردت بقاءها.

**لا مصادقة ولا حدود.** أي شخص يصل للرابط يستطيع رفع ملفات وتشغيل رندر.
قبل أي نشر عام: ضع مصادقة، وحدّاً لحجم الملف المرفوع، وحدّاً لعدد المهام
لكل مستخدم. الخادم الآن مبنيّ للاستعمال الخاص.

**الخطوط.** ملفا خط ثمانية داخل `public/fonts/` ويُنشران مع الصورة. رخصة
ثمانية تمنع إعادة الاستضافة العلنية — الخادم يقدّمهما لكل زائر عبر
`/fonts/...`. للاستعمال الخاص لا إشكال؛ قبل النشر العام راجع الرخصة.

**رخصة Remotion.** مجانية للأفراد وللشركات الربحية حتى ثلاثة موظفين؛ فوق
ذلك تحتاج Company License. راجع `node_modules/remotion/LICENSE.md`.

## متغيّرات البيئة

| المتغيّر | الافتراضي | الوظيفة |
|---|---|---|
| `PORT` | `5174` | منفذ الخادم |

## هيكل ما يُنشَر

```
Dockerfile          صورة التشغيل
src/server/         الخادم والطابور
src/ui/             مصدر الواجهة (تُبنى إلى dist-ui)
src/templates/      القوالب
public/fonts/       خط ثمانية — مطلوب، وبدونه يسقط التصميم على خط بديل
public/previews/    عيّنات المعرض
```
